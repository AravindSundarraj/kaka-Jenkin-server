pipeline{
    agent any
    stages {
        stage('init'){
            steps{
                echo 'Jenkins pipeline test'
            }
        }
        stage('build'){
            steps{
                echo 'build the pipeline'
            }
        }
        stage('deploy'){
            steps{
                echo "deploy the pipeline"
            }
        }

    }
}